// Package model manages the fixed model manifest (name -> Hugging Face URL,
// SHA256, size), lazy downloads with progress reporting, SHA256 verification,
// .partial cleanup, and listing/deleting downloaded models.
package model
